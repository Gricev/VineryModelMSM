# Базовый поток + обработка кадров
opencv-python>=4.9
numpy>=1.26

# Модели детекции/классификации (раскомментируйте, когда дойдёте до инференса).
# ultralytics — YOLOv8 для детекции куста (cam1) и грозди/листа (cam2).
# ultralytics>=8.2
# torch>=2.2

# БД: SQLite встроен в стандартную библиотеку Python (модуль sqlite3),
# отдельная установка не нужна. Для перехода на PostgreSQL:
# psycopg[binary]>=3.1
