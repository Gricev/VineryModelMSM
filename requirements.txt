# Базовый поток + обработка кадров
opencv-python>=4.9
numpy>=1.26

# Генерация синтетики и работа с изображениями (vinery.training.synthetic).
pillow>=10.0

# Обучение/инференс моделей (раскомментируйте, когда дойдёте до train/track).
# ultralytics — YOLOv8/v11: детекция+трекинг ствола лозы (cam1), детекция листа/грозди (cam2).
# Тянет за собой torch. Подготовка датасета (synthetic + yolo_dataset) работает и без них.
# ultralytics>=8.2
# torch>=2.2
# timm — классификаторы сорта/болезни и регрессор зрелости (cam2).
# timm>=1.0
# SAM2 — авто-предразметка стволов (vinery.training.sam2_prelabel): маска -> YOLO-бокс.
# Тянет torch; гоните на GPU. Конвертация маска->бокс и её тест работают без них.
# torch>=2.2
# sam2 @ git+https://github.com/facebookresearch/sam2.git

# БД: SQLite встроен в стандартную библиотеку Python (модуль sqlite3),
# отдельная установка не нужна. Для перехода на PostgreSQL:
# psycopg[binary]>=3.1
